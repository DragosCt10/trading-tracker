/**
 * Cookie keys for "last selected account" preference (mode + index only, no ids).
 * Used so refresh restores the same account without storing sensitive data.
 */
export const LAST_ACCOUNT_MODE_COOKIE = 'tt_last_mode';
export const LAST_ACCOUNT_INDEX_COOKIE = 'tt_last_index';

export const LAST_ACCOUNT_COOKIE_MAX_AGE_DAYS = 365;
